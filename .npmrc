enable-pre-post-scripts=true
auto-install-peers=true
strict-peer-dependencies=false
node-linker=isolated

# Allow esbuild to run its install scripts in this dev environment.
# The CLI itself doesn't ship to users; users running `pnpm install` in
# generated monorepos won't be affected.
onlyBuiltDependencies[]=esbuild
